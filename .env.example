# Target after a deploy. Local default is http://127.0.0.1:8000
BASE_URL=https://your-production-domain.example

# Required for signed-in regression (admin dashboard + prices)
BB_ADMIN_EMAIL=
BB_ADMIN_PASSWORD=

# Optional non-admin shopper. Used to prove customers cannot open /admin
BB_CUSTOMER_EMAIL=
BB_CUSTOMER_PASSWORD=
